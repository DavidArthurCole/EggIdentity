name: CI

on:
  push:
    branches: [master, main]
  pull_request:

jobs:
  build-test:
    uses: ./.github/workflows/dotnet-ci.yml
    with:
      solution: SyncKit.slnx
      dotnet-version: "10.0.x"
